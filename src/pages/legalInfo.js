const MONGODB_URI = process.env.MONGODB_URI
const LegalInfo = () => {
    console.log(MONGODB_URI)
    return (
        <>

            <h1>Esto es un prototipo</h1>
            <h3>No se hace uso de ningún dato</h3>
        </>
    )
}

export default LegalInfo